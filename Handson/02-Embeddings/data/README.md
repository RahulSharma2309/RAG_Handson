# Embeddings Data Workspace

Use this folder for Phase 2 embedding artifacts.

## Suggested structure

- `chunk_inputs/` — input chunk JSONL files from chunking phase
- `embedding_artifacts/` — embedded vectors/manifests/cache outputs
- `quality_reports/` — evaluation reports, benchmark outputs, and diagnostics

Keep raw chunk inputs immutable and write new embedding versions with clear timestamps.
